@extends('adminlte::page')

@section('adminlte_css')
    @parent
    <style>
        .navbar-badge-user-baru {
            position: absolute;
            top: 5px;
            right: -5px;
            background-color: #dc3545;
            color: white;
            border-radius: 50%;
            width: 20px;
            height: 20px;
            font-size: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
        }
    </style>
@endsection

@section('content')
    @parent
@endsection

@section('js')
@parent
<script>
$(function () {
    // Cari menu "Aktivasi User" di navbar
    const $menu = $('.navbar-nav .nav-link[href$="admin/user-baru"]');
    if ($menu.length && {{ $userBaruCount ?? 0 }} > 0) {
        $menu.append('<span class="navbar-badge-user-baru" title="Ada {{ $userBaruCount }} user baru">' + {{ $userBaruCount }} + '</span>');
        $('[data-toggle="tooltip"]').tooltip();
    }
});
</script>
@stop